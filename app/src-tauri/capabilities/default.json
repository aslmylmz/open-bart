{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal offline capability set: webview core, the fullscreen + always-on-top window setters the kiosk lock uses (issue 44), native file dialogs, and scoped local file read/write for study.json (SPEC §10). No shell, no internet HTTP.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-fullscreen",
    "core:window:allow-set-always-on-top",
    "dialog:allow-open",
    "dialog:allow-save",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file"
  ]
}
