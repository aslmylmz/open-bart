# Core dependencies for the BART scoring engine
# Install with: pip install -r requirements.txt

# Scientific computing
numpy>=1.26
scipy>=1.12

# Schema validation
pydantic>=2.6

# ── Optional: for scripts/ ──────────────────────────────────────────────
# Monte Carlo simulation (scripts/monte_carlo_ev.py)
matplotlib>=3.8

# Synthetic data generation (scripts/generate_synthetic.py)
pandas>=2.2
