# Core dependencies for the BART scoring engine.
# pyproject.toml is the authority — prefer:  pip install -e .
# This file mirrors the core dependencies for quick environment setup.

# Scientific computing (the engine is scipy-free)
numpy>=1.26

# Schema validation
pydantic>=2.6

# ── Optional: for scripts/ ──────────────────────────────────────────────
# Figures, synthetic datasets, and distribution utilities are extras:
#   pip install -e ".[scripts]"        (matplotlib, pandas, scipy)
